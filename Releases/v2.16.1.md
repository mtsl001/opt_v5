# OptDash Release Note - v2.16.1 (March 24, 2026)

This hotfix release directly trails `v2.16.0`, comprehensively clearing the final 9 `ifix_plan.md` internal logic issues identified during the deepest layer of execution review (Pass 4).

## 1. Zero-Signal Fallback & Exception Guards
- **GEX Pct Denominator Guard:** Handled a silent data-drop bug in `get_net_gex`. The application now safely assigns `None` rather than triggering a Python `TypeError` inside `round(None, 1)` when database caches return empty peaks.
- **Direction Exception Parity:** Expanded the main `get_directional_bias` failure branch to properly emit trailing variables (`vex_data`, `conviction`, `unique_source_count`). This safeguards upper-level destructuring in standard API execution loops.

## 2. Environment Gates & Threshold Arrays
- **Semantical Penalty Accounting:** Re-factored the core `_raw_max` scoring gate in `environment.py` to assert mathematical subtractions directly when encountering valid explicit penalties (`BACKWARDATION`).
- **Synchronous Gate Counting:** Fixed an off-by-1 synchronization bug in the NO_GO counter. `tracker.py` now statically includes the live un-committed gate verdict directly into SQLite's memory stream, firing exits accurately 100% on time.

## 3. SQL & Recommender Integrations
- **DuckDB Date Boundaries:** Secured the `_nearest_expiry` pointer by locking its bound parameters using `CAST(? AS DATE)`. This ensures seamless cross-database boundary scanning across all timezone configurations.
- **RR Floor Locking:** Prevented `AI_TARGET_MULT` from automatically bridging Risk/Reward safety algorithms via `max()`. The Recommender will now intentionally fail and skip targets when required IV expansions surpass threshold profiles.
- **HV20 Lag Syntax:** Cemented `LAG()` behavior in `iv.py` to strictly build from `OVER (ORDER BY ... ASC)`. This permanently anchors window distributions to correct ascending volatility curves.
- **Weights Alignment:** Cleaned up structural duplication on the `unique_source_count` penalty matrix safely inside standard momentum branches.

---
**Files affected:**
- `optdash/analytics/gex.py`
- `optdash/analytics/environment.py`
- `optdash/analytics/iv.py`
- `optdash/ai/recommender.py`
- `optdash/ai/direction.py`
- `optdash/ai/tracker.py`
